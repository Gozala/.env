export NVM_DIR="$HOME/.nvm"
. "/usr/local/opt/nvm/nvm.sh"

# Customize to your needs...
export PATH="${HOME}/.lein/bin:${HOME}/.local/bin:/usr/local/bin:/usr/local/sbin:${PATH}"

# Add npm install stuff to a path
export PATH="${PWD}/node_modules/.bin:${PATH}"
# Add moz-git-tools
export PATH="${HOME}/Projects/moz-git-tools:${PATH}"

# Customize to your needs...
export PATH="${HOME}/.lein/bin:${HOME}/.local/bin:/usr/local/bin:/usr/local/sbin:${PATH}"

# Add npm install stuff to a path
export PATH="${PWD}/node_modules/.bin:${PATH}"
# Add moz-git-tools
export PATH="${HOME}/Projects/moz-git-tools:${PATH}"

export GOPATH="${HOME}/.go"
export PATH="${PATH}:${GOPATH}/bin"

# Set up python
# export PATH="/usr/local/Cellar/python:${PATH}"
export NGEN_TEMPLATE="local"

# Set up clojurescript
export CLOJURESCRIPT_HOME="${HOME}/Projects/clojurescript"
export PATH="${CLOJURESCRIPT_HOME}/bin:${PATH}"

export CFX_ROOT="/Users/gozala/Projects/addon-sdk/"

# Set up Yarn
export YARNPATH="`yarn global bin`"
export PATH="${PATH}:${YARNPATH}/bin"

# Set up rust
export CARGO_PATH="$HOME/.cargo/bin"
export PATH="$CARGO_PATH:$PATH"

